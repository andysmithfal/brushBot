// run the sketch 'offline' (no serial comms) for testing purposes
  boolean conf_run_offline = true;

// serial/com port address 
    //this is all automatic now

// screen resolution

  int conf_screen_x = 1280;
  int conf_screen_y = 800;

// brush hover height (0 = highest, 180 = lowest)
  int brush_hover_height = 100;